[project]
name = "btg-pix-batch"
version = "0.1.0"
description = "BTG Pactual Empresas - Pix Batch Payment CLI"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28",
]

[project.scripts]
pix-batch = "pix_batch:main"
