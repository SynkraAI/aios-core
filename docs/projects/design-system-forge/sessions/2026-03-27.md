# Session 2026-03-27

## Projeto
- **Nome:** Design System Forge v2
- **INDEX.md:** `docs/projects/design-system-forge/INDEX.md`

## O que foi feito

### 1. Análise comparativa (contexto)
- Comparou stcgroup.com.br (WordPress/Kubio) vs stc-redesign.vercel.app (código custom)
- Entendeu o pipeline do cara do grupo AIOS: Playwright scraping → Pattern Library → Claude Code → Site final
- Identificou que o dissect-artifact.cjs precisava de modo clone completo

### 2. dissect-artifact.cjs — modo --clone (maior entrega)
- Network interception: captura fontes, imagens, CSS de CDN (resolve cross-origin)
- SVG extraction: antes eram ignorados (linha 339 skipava svg/path)
- Background image download com deduplicação
- Clean HTML generation com URL rewriting (local paths)
- srcset stripping, /_next/image decoding
- Download de URLs residuais no HTML
- Screenshot timeout 30s → 60s
- Testado com circle.so/br: 122 imagens locais, 16 SVGs, 1 fonte, 153 CSS, 477 CSS vars

### 3. Skills e quest
- SKILL.md reescrito como orquestrador interativo (8 passos guiados com AskUserQuestion)
- Tabela de 15 fontes de animação (Aceternity UI, GSAP, Framer Motion, etc.)
- Quest pack: item 2.5 obrigatório "Buscar animações premium"
- Command map atualizado com fontes de animação

### 4. Epic DSF2 — 4 stories
- DSF2-1 Done: skill orquestradora (`design-system-forge/SKILL.md` v2.0)
- DSF2-2 Done: skill scaffold (`design-system-scaffold/` — 3 scripts + SKILL.md)
- DSF2-3 Draft: Storybook builder (próxima)
- DSF2-4 Draft: catálogo global (última)

### 5. Skill design-system-scaffold (NOVA)
- `tokens-to-tailwind.mjs` — tokens.yaml → tailwind.config.ts (testado: 20 cores, 32 animações)
- `tokens-to-css.mjs` — tokens.yaml → CSS custom properties (testado: 20 cores, 2 fontes, 50 custom props)
- `scaffold-project.mjs` — CLI completo com dry-run, skip flags, copy assets (testado: 311 files)

## Agente/Squad em uso
@dev (implementação direta, sem squad)

## Arquivos para contexto (próximo Claude DEVE ler)
- `skills/design-system-forge/SKILL.md` — orquestrador interativo v2.0
- `skills/design-system-scaffold/SKILL.md` — skill de scaffold
- `squads/design/scripts/dissect-artifact.cjs` — motor de extração com --clone
- `docs/stories/active/DSF2-3.criar-skill-storybook.story.md` — próxima story
- `docs/stories/epics/EPIC-DSF2-design-system-forge-v2.md` — epic overview
- `memory/project_design-system-forge-v2.md` — decisões arquiteturais

## Decisões tomadas
- Design systems em `~/CODE/design-systems/{nome}/`
- Catálogo global em `~/CODE/design-systems/CATALOG.md`
- Storybook 8 + Chromatic
- Interação via AskUserQuestion a cada passo
- 3 skills separadas: forge (extração) + scaffold (setup) + storybook (componentes) + catalog
- Scripts são 100% agnósticos (recebem path, não sabem nada do site específico)
- Clone mode é opt-in via `--clone` (não quebra modo normal)

## Próximo passo exato
Implementar DSF2-3: `skills/design-system-storybook/` — lê components.json + extracted-css.json + tokens.yaml e gera componentes React (atoms/molecules/organisms) + stories do Storybook. Guia o usuário com AskUserQuestion a cada componente.

## Arquivos modificados não commitados
Nossos arquivos parecem commitados (verificado via git show HEAD). Restam mudanças não-relacionadas: squad-creator deletes, .DS_Store, kaizen daily, session.json.
