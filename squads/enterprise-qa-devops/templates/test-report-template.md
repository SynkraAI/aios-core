# ${title}

**Generated:** ${date}
**Execution:** [${execution_key}](https://${ATLASSIAN_DOMAIN}/browse/${execution_key})
**Version:** ${version}
**Test Plan:** [${test_plan}](https://${ATLASSIAN_DOMAIN}/browse/${test_plan})

---

## Executive Summary

<ac:structured-macro ac:name="status">
  <ac:parameter ac:name="title">${status}</ac:parameter>
  <ac:parameter ac:name="colour">${status === 'PASSED' ? 'Green' : 'Red'}</ac:parameter>
</ac:structured-macro>

| Metric | Value |
|--------|-------|
| **Total Tests** | ${total} |
| **Passed** | ${passed} ✅ |
| **Failed** | ${failed} ❌ |
| **Skipped** | ${skipped} ⏭️ |
| **Pass Rate** | ${pass_rate}% |
| **Duration** | ${duration} |
| **Coverage** | ${coverage}% |

---

## Test Results by Category

### By Component

| Component | Passed | Failed | Total | Rate |
|-----------|--------|--------|-------|------|
${components.map(c => `| ${c.name} | ${c.passed} | ${c.failed} | ${c.total} | ${c.rate}% |`).join('\n')}

### By Test Type

| Type | Passed | Failed | Total |
|------|--------|--------|-------|
| Unit Tests | ${unit_passed} | ${unit_failed} | ${unit_total} |
| Integration Tests | ${integration_passed} | ${integration_failed} | ${integration_total} |
| E2E Tests | ${e2e_passed} | ${e2e_failed} | ${e2e_total} |

---

## Failed Tests

${failed > 0 ? `
<ac:structured-macro ac:name="warning">
  <ac:rich-text-body>
    <p><strong>${failed} tests require attention</strong></p>
  </ac:rich-text-body>
</ac:structured-macro>

| Test | Error | Component |
|------|-------|-----------|
${failed_tests.map(t => `| [${t.key}](${t.url}) | ${t.error} | ${t.component} |`).join('\n')}
` : `
<ac:structured-macro ac:name="info">
  <ac:rich-text-body>
    <p>No failed tests! 🎉</p>
  </ac:rich-text-body>
</ac:structured-macro>
`}

---

## Coverage Analysis

### Requirement Coverage

| Status | Count | Percentage |
|--------|-------|------------|
| Covered | ${requirements_covered} | ${requirements_coverage}% |
| Not Covered | ${requirements_not_covered} | ${100 - requirements_coverage}% |

### Uncovered Requirements

${uncovered_requirements.length > 0 ? `
| Key | Summary | Priority |
|-----|---------|----------|
${uncovered_requirements.map(r => `| [${r.key}](${r.url}) | ${r.summary} | ${r.priority} |`).join('\n')}
` : 'All requirements have test coverage.'}

---

## Environment

| Property | Value |
|----------|-------|
| Environment | ${environment} |
| Browser | ${browser} |
| OS | ${os} |
| Build | ${build_number} |
| Commit | ${commit_sha} |

---

## Trend Analysis

### Pass Rate Trend (Last 5 Runs)

| Run | Date | Pass Rate | Trend |
|-----|------|-----------|-------|
${trends.map((t, i) => `| ${i + 1} | ${t.date} | ${t.rate}% | ${t.trend} |`).join('\n')}

---

## Action Items

${failed > 0 ? `
1. [ ] Review failed test cases
2. [ ] Create bug reports for new failures
3. [ ] Update test data if needed
4. [ ] Re-run failed tests after fixes
` : `
1. [x] All tests passing
2. [ ] Review coverage gaps
3. [ ] Prepare for release
`}

---

## Links

- [Test Execution in Xray](https://${ATLASSIAN_DOMAIN}/browse/${execution_key})
- [Test Plan](https://${ATLASSIAN_DOMAIN}/browse/${test_plan})
- [Jira Filter - Failed Tests](https://${ATLASSIAN_DOMAIN}/issues/?jql=key in (${failed_test_keys}))
- [CI Build #${build_number}](${ci_url})

---

*Generated by AIOS Enterprise QA DevOps Squad*
*Template: test-report-template v1.0*
