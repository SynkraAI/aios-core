# Bug Report: ${title}

**Reporter:** ${reporter}
**Date:** ${date}
**Severity:** ${severity}
**Priority:** ${priority}

---

## Overview

| Field | Value |
|-------|-------|
| **Status** | ${status} |
| **Component** | ${component} |
| **Affects Version** | ${affects_version} |
| **Found In** | ${found_in} |
| **Environment** | ${environment} |

---

## Description

${description}

---

## Steps to Reproduce

${steps.map((step, i) => `${i + 1}. ${step}`).join('\n')}

---

## Expected Result

${expected_result}

---

## Actual Result

${actual_result}

---

## Environment Details

| Property | Value |
|----------|-------|
| Browser | ${browser} |
| Browser Version | ${browser_version} |
| Operating System | ${os} |
| Device | ${device} |
| Screen Resolution | ${resolution} |
| Network | ${network} |

---

## Additional Information

### Error Messages

```
${error_message}
```

### Stack Trace

<ac:structured-macro ac:name="code">
  <ac:parameter ac:name="language">text</ac:parameter>
  <ac:plain-text-body><![CDATA[
${stack_trace}
  ]]></ac:plain-text-body>
</ac:structured-macro>

### Console Logs

<ac:structured-macro ac:name="code">
  <ac:parameter ac:name="language">javascript</ac:parameter>
  <ac:plain-text-body><![CDATA[
${console_logs}
  ]]></ac:plain-text-body>
</ac:structured-macro>

---

## Attachments

${attachments.map(a => `- [${a.name}](${a.url})`).join('\n')}

---

## Impact Analysis

### Affected Features

${affected_features.map(f => `- ${f}`).join('\n')}

### Workaround

${workaround || 'No workaround available.'}

---

## Related Items

| Type | Key | Summary |
|------|-----|---------|
${related_items.map(item => `| ${item.type} | [${item.key}](${item.url}) | ${item.summary} |`).join('\n')}

---

## Test Coverage

| Test Key | Test Name | Status |
|----------|-----------|--------|
${test_coverage.map(t => `| [${t.key}](${t.url}) | ${t.name} | ${t.status} |`).join('\n')}

---

## Investigation Notes

${investigation_notes || '_No investigation notes yet._'}

---

## Resolution

**Fix Version:** ${fix_version}
**Resolution:** ${resolution}
**Resolved By:** ${resolved_by}
**Resolution Date:** ${resolution_date}

### Root Cause

${root_cause || '_Pending investigation._'}

### Solution

${solution || '_Pending fix._'}

---

*Generated by AIOS Enterprise QA DevOps Squad*
*Template: bug-report-template v1.0*
