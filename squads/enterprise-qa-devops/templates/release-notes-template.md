# Release ${version}

**Release Date:** ${release_date}
**Environment:** ${environment}
**Build:** #${build_number}

---

## Overview

${overview}

<ac:structured-macro ac:name="status">
  <ac:parameter ac:name="title">${release_status}</ac:parameter>
  <ac:parameter ac:name="colour">Green</ac:parameter>
</ac:structured-macro>

---

## What's New

### New Features ✨

${features.map(f => `
#### ${f.title}

${f.description}

- **Jira:** [${f.key}](${f.url})
- **Component:** ${f.component}

${f.details || ''}
`).join('\n')}

---

## Bug Fixes 🐛

| Key | Summary | Severity | Component |
|-----|---------|----------|-----------|
${bugfixes.map(b => `| [${b.key}](${b.url}) | ${b.summary} | ${b.severity} | ${b.component} |`).join('\n')}

---

## Improvements 🔧

${improvements.map(i => `- **${i.title}:** ${i.description} ([${i.key}](${i.url}))`).join('\n')}

---

## Breaking Changes ⚠️

${breaking_changes.length > 0 ? `
<ac:structured-macro ac:name="warning">
  <ac:rich-text-body>
    <p><strong>This release contains breaking changes. Please review before upgrading.</strong></p>
  </ac:rich-text-body>
</ac:structured-macro>

${breaking_changes.map(c => `
### ${c.title}

**Impact:** ${c.impact}

**Migration Steps:**
${c.migration_steps.map((s, i) => `${i + 1}. ${s}`).join('\n')}
`).join('\n')}
` : '_No breaking changes in this release._'}

---

## Deprecations

${deprecations.length > 0 ? `
| Feature | Deprecated | Removal Version | Alternative |
|---------|------------|-----------------|-------------|
${deprecations.map(d => `| ${d.feature} | ${version} | ${d.removal_version} | ${d.alternative} |`).join('\n')}
` : '_No deprecations in this release._'}

---

## Known Issues

${known_issues.length > 0 ? `
| Key | Summary | Workaround | Target Fix |
|-----|---------|------------|------------|
${known_issues.map(i => `| [${i.key}](${i.url}) | ${i.summary} | ${i.workaround} | ${i.target_fix} |`).join('\n')}
` : '_No known issues._'}

---

## Test Results

### Summary

| Metric | Value |
|--------|-------|
| Total Tests | ${tests_total} |
| Passed | ${tests_passed} ✅ |
| Failed | ${tests_failed} ❌ |
| Pass Rate | ${pass_rate}% |
| Coverage | ${coverage}% |

### Test Execution

- [Test Execution](${test_execution_url})
- [Coverage Report](${coverage_report_url})

---

## Deployment Notes

### Prerequisites

${prerequisites.map(p => `- ${p}`).join('\n')}

### Configuration Changes

${config_changes.length > 0 ? `
| Setting | Old Value | New Value | Required |
|---------|-----------|-----------|----------|
${config_changes.map(c => `| \`${c.setting}\` | ${c.old_value} | ${c.new_value} | ${c.required ? 'Yes' : 'No'} |`).join('\n')}
` : '_No configuration changes required._'}

### Database Migrations

${migrations.length > 0 ? `
| Migration | Description | Reversible |
|-----------|-------------|------------|
${migrations.map(m => `| ${m.name} | ${m.description} | ${m.reversible ? 'Yes' : 'No'} |`).join('\n')}
` : '_No database migrations in this release._'}

### Rollback Procedure

In case of issues:

${rollback_steps.map((s, i) => `${i + 1}. ${s}`).join('\n')}

---

## API Changes

### New Endpoints

${new_endpoints.length > 0 ? `
| Method | Endpoint | Description |
|--------|----------|-------------|
${new_endpoints.map(e => `| \`${e.method}\` | \`${e.path}\` | ${e.description} |`).join('\n')}
` : '_No new endpoints._'}

### Modified Endpoints

${modified_endpoints.length > 0 ? `
| Endpoint | Change | Breaking |
|----------|--------|----------|
${modified_endpoints.map(e => `| \`${e.path}\` | ${e.change} | ${e.breaking ? '⚠️ Yes' : 'No'} |`).join('\n')}
` : '_No modified endpoints._'}

### Removed Endpoints

${removed_endpoints.length > 0 ? `
<ac:structured-macro ac:name="warning">
  <ac:rich-text-body>
    <p>The following endpoints have been removed:</p>
  </ac:rich-text-body>
</ac:structured-macro>

| Endpoint | Alternative |
|----------|-------------|
${removed_endpoints.map(e => `| \`${e.path}\` | ${e.alternative} |`).join('\n')}
` : '_No removed endpoints._'}

---

## Contributors

Thanks to everyone who contributed to this release:

${contributors.map(c => `- ${c.name} (@${c.username})`).join('\n')}

---

## Links

- [Jira Release](https://${ATLASSIAN_DOMAIN}/projects/${project}/versions/${version_id})
- [CI Build #${build_number}](${ci_url})
- [Docker Image](${docker_image})
- [Documentation](${docs_url})
- [API Reference](${api_docs_url})

---

*Generated by AIOS Enterprise QA DevOps Squad*
*Template: release-notes-template v1.0*
