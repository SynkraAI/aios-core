# ${sprint_name} Summary

**Team:** ${team}
**Duration:** ${start_date} → ${end_date}
**Generated:** ${generated_date}

---

## Sprint Goal

> ${sprint_goal}

<ac:structured-macro ac:name="status">
  <ac:parameter ac:name="title">${goal_status}</ac:parameter>
  <ac:parameter ac:name="colour">${goal_status === 'Achieved' ? 'Green' : goal_status === 'Partial' ? 'Yellow' : 'Red'}</ac:parameter>
</ac:structured-macro>

---

## Velocity & Burndown

### Velocity Metrics

| Metric | Planned | Completed | Variance |
|--------|---------|-----------|----------|
| **Story Points** | ${planned_points} | ${completed_points} | ${variance_points} |
| **Stories** | ${planned_stories} | ${completed_stories} | ${variance_stories} |
| **Tasks** | ${planned_tasks} | ${completed_tasks} | ${variance_tasks} |
| **Bugs Fixed** | ${bugs_planned} | ${bugs_fixed} | ${bugs_variance} |

### Velocity Trend

| Sprint | Points Completed | Team Size | Velocity/Person |
|--------|-----------------|-----------|-----------------|
${velocity_trend.map(v => `| ${v.sprint} | ${v.points} | ${v.team_size} | ${v.velocity_per_person} |`).join('\n')}

---

## Completion by Type

| Type | Total | Done | In Progress | Not Started |
|------|-------|------|-------------|-------------|
| Stories | ${stories_total} | ${stories_done} | ${stories_progress} | ${stories_todo} |
| Tasks | ${tasks_total} | ${tasks_done} | ${tasks_progress} | ${tasks_todo} |
| Bugs | ${bugs_total} | ${bugs_done} | ${bugs_progress} | ${bugs_todo} |
| Spikes | ${spikes_total} | ${spikes_done} | ${spikes_progress} | ${spikes_todo} |

---

## Completed Work

### Features Delivered

${completed_features.map(f => `
#### ${f.key}: ${f.summary}

- **Story Points:** ${f.points}
- **Assignee:** ${f.assignee}
- **Status:** ✅ Done

${f.description}
`).join('\n')}

### Bug Fixes

| Key | Summary | Severity | Fixed By |
|-----|---------|----------|----------|
${completed_bugs.map(b => `| [${b.key}](${b.url}) | ${b.summary} | ${b.severity} | ${b.assignee} |`).join('\n')}

---

## Incomplete Work

<ac:structured-macro ac:name="warning">
  <ac:rich-text-body>
    <p><strong>${incomplete_count} items rolled over to next sprint</strong></p>
  </ac:rich-text-body>
</ac:structured-macro>

| Key | Summary | Points | Reason |
|-----|---------|--------|--------|
${incomplete_items.map(i => `| [${i.key}](${i.url}) | ${i.summary} | ${i.points} | ${i.reason} |`).join('\n')}

---

## Quality Metrics

### Test Results

| Metric | Value | Target | Status |
|--------|-------|--------|--------|
| Tests Executed | ${tests_executed} | - | - |
| Tests Passed | ${tests_passed} | ≥95% | ${tests_passed_pct >= 95 ? '✅' : '❌'} |
| Tests Failed | ${tests_failed} | ≤5% | ${tests_failed_pct <= 5 ? '✅' : '❌'} |
| Coverage | ${test_coverage}% | ≥80% | ${test_coverage >= 80 ? '✅' : '❌'} |

### Code Quality

| Metric | Value | Target | Status |
|--------|-------|--------|--------|
| Code Coverage | ${code_coverage}% | ≥80% | ${code_coverage >= 80 ? '✅' : '❌'} |
| Technical Debt | ${tech_debt} hrs | <20 hrs | ${tech_debt < 20 ? '✅' : '❌'} |
| Code Smells | ${code_smells} | <50 | ${code_smells < 50 ? '✅' : '❌'} |
| Security Issues | ${security_issues} | 0 | ${security_issues === 0 ? '✅' : '❌'} |

---

## Team Performance

### Contribution by Team Member

| Member | Stories | Points | PRs Merged | Reviews |
|--------|---------|--------|------------|---------|
${team_contributions.map(m => `| ${m.name} | ${m.stories} | ${m.points} | ${m.prs} | ${m.reviews} |`).join('\n')}

---

## Risks & Blockers

### Blockers Encountered

| Blocker | Duration | Impact | Resolution |
|---------|----------|--------|------------|
${blockers.map(b => `| ${b.description} | ${b.duration} | ${b.impact} | ${b.resolution} |`).join('\n')}

### Risks Identified

| Risk | Probability | Impact | Mitigation |
|------|-------------|--------|------------|
${risks.map(r => `| ${r.description} | ${r.probability} | ${r.impact} | ${r.mitigation} |`).join('\n')}

---

## Retrospective Notes

### What Went Well 🌟

${went_well.map(w => `- ${w}`).join('\n')}

### What Could Be Improved 🔧

${improvements.map(i => `- ${i}`).join('\n')}

### Action Items 📋

| Action | Owner | Due Date | Status |
|--------|-------|----------|--------|
${action_items.map(a => `| ${a.action} | ${a.owner} | ${a.due_date} | ${a.status} |`).join('\n')}

---

## Links

- [Sprint Board](${board_url})
- [Sprint Report in Jira](${jira_report_url})
- [Test Execution](${test_execution_url})
- [Previous Sprint Summary](${previous_sprint_url})

---

*Generated by AIOS Enterprise QA DevOps Squad*
*Template: sprint-summary-template v1.0*
