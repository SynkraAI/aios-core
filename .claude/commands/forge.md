Read the skill at `~/aios-core/skills/forge/SKILL.md` and execute it against the current working directory. This is the Forge Pipeline Runner — from idea to deploy, no shallow shortcuts. It orchestrates all AIOS agents automatically in smart sequence with checkpoints, error recovery and ecosystem context. Use when you want to create an app, feature or fix without managing agents manually. Pass user arguments to the skill.