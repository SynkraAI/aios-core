Read the skill at `~/aios-core/skills/quest/SKILL.md` and execute it against the current working directory. This is the Quest Engine — it diagnoses the project state, sets up what's missing, starts the dashboard hub, creates the gamified checklist, and guides you step by step through the entire development lifecycle. Works with any project type via packs.