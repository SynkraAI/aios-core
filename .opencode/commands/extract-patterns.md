---
description: "Extract and document code patterns from the codebase. Analyzes code via AST and regex to detect comm..."
agent: "aios-master"
---

Execute a skill **extract-patterns** com os seguintes argumentos: $ARGUMENTS

Instruções:
1. Usar a ferramenta nativa `skill` para executar: `*extract-patterns`
2. Passar os argumentos: $ARGUMENTS
3. Permanecer como @aios-master durante a execução.

---
*AIOS Skill Command - Sincronizado para /extract-patterns*
