---
description: "> Create Code Mode workflows that execute in Docker MCP sandbox for ~98.7% token savings."
agent: "aios-master"
---

Execute a skill **mcp-workflow** com os seguintes argumentos: $ARGUMENTS

Instruções:
1. Usar a ferramenta nativa `skill` para executar: `*mcp-workflow`
2. Passar os argumentos: $ARGUMENTS
3. Permanecer como @aios-master durante a execução.

---
*AIOS Skill Command - Sincronizado para /mcp-workflow*
